import React from 'react';

/**
 * INTERNAL CHAT WIDGET - DISABLED FOR PERFORMANCE OPTIMIZATION
 * 
 * This component has been completely disabled to optimize system performance.
 * Real-time chat features were consuming significant resources and bandwidth.
 * 
 * Removed features:
 * - Real-time global chat
 * - Private direct messaging
 * - Firebase Firestore subscriptions
 * - User presence detection
 */

const InternalChatWidget: React.FC<any> = () => {
  // Component disabled - returns null
  return null;
};

export default InternalChatWidget;
